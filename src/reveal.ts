/**
 * Daily-reveal series anchor (IST). Edit `REVEAL_SERIES_START_ISO` only; run `npm run build:data`.
 */
export const REVEAL_SERIES_START_ISO = "2026-05-07T00:00:00+05:30";

export const REVEAL_SERIES_START_MS = Date.parse(REVEAL_SERIES_START_ISO);
